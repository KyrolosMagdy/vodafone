import { styled } from "@mui/material";

export const StyledFormWrapper = styled('form')(({ theme }) => ({
    marginTop: '1rem',
    display: 'grid',
    gridGap: '0.7rem',
    width: '100%'
}))